# Зависимости FreeConnect. Нижняя граница версий — то, на чём собрано и проверено;
# верхнюю не фиксируем, чтобы ловить проблемы совместимости заранее.

# Соединение Telegram: локальный SOCKS5 -> WebSocket к веб-каналу Telegram.
websockets>=15.0        # клиент WebSocket (принимает готовый сокет + SNI)
cryptography>=44.0      # AES-256-CTR: разбор obfuscated2 init-пакета -> номер ДЦ

# GUI и трей. В юнит-тестах не нужны (app.py импортирует их лениво), но входят
# в реальную сборку — держим здесь, чтобы окружение было воспроизводимым.
pywebview>=6.0          # окно на WebView2, без Electron
pystray>=0.19           # иконка в системном трее
pillow>=10.0            # иконки для pystray
